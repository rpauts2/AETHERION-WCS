using WcsInfinity.Races;
// using CounterStrikeSharp.API; // подключить при сборке на сервере

namespace WcsInfinity.Core;

// Реализация абстракции движка через CounterStrikeSharp.
// Здесь сосредоточена вся работа с pawn/CCSPlayerController, чтобы расы
// оставались чистыми и тестируемыми.
public class Cs2EngineApi : IEngineApi
{
    // NOTE: тела методов используют Utilities.GetPlayerFromSlot(slot).PlayerPawn
    // и т.п. На этапе MVP оставлены заглушки-сигнатуры — заполняются при
    // интеграции с конкретной версией CounterStrikeSharp API.

    public void SetHealth(int slot, int hp) { /* pawn.Health = hp; SetStateChanged */ }
    public void AddHealth(int slot, int amount) { /* pawn.Health += amount */ }
    public void SetSpeed(int slot, float multiplier) { /* pawn.VelocityModifier = multiplier */ }
    public void SetGravity(int slot, float multiplier) { /* pawn.GravityScale = multiplier */ }
    public void Teleport(int slot, float x, float y, float z) { /* pawn.Teleport(...) */ }
    public void GiveWeaponInfiniteAmmo(int slot, string weapon) { /* give + set Clip1=max each shot */ }
    public void SpawnParticle(string effect, float x, float y, float z) { /* CreateEntityByName particle */ }
    public void PrintToCenter(int slot, string msg) { /* player.PrintToCenter(msg) */ }
    public void PrintToChat(int slot, string msg) { /* player.PrintToChat(msg) */ }
    public (float x, float y, float z) GetPosition(int slot) => (0, 0, 0);
    public (float x, float y, float z) GetAimEndPosition(int slot) => (0, 0, 0);
}
